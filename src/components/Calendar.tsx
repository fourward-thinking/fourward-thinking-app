import { Col, Container } from 'react-bootstrap';

const Calendar = () => (
  <Container>
    <Col className="text-center">
      Create Session
    </Col>
  </Container>
);

export default Calendar;
